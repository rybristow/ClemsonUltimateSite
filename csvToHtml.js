function convertToArray(sCSV) {
  var fs = require("fs");
  var text = fs.readFileSync("./mytext.txt");
  var textByLine = text.split("\n");
  var players[textByLine.count][2];

  for (player in players) {
    player = textByLine[i].split(",");
  }


}
